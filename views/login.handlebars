<!DOCTYPE html>
<html>
    <head>
        <title>Login</title>    
        <link rel="stylesheet" href="/styles/login.css" />
        <link rel="stylesheet" href="/styles/forms.css" />
        <link href="https://fonts.googleapis.com/css?family=Amatic+SC|Lato" rel="stylesheet">
    </head>
    <body>
        <div id="login-wrap">
            <form method="POST" class="login-form form">
                <h1 class="logo">Freelancers: Log Into Your Profile Here</h1>
                {{#if loginMessage}}
                <div class="errorMessage">
                    {{loginMessage}}
                </div>
                {{/if}}
                <div class="input-wrap">
                    <label for="username">Username</label>
                    <input type="text" name="username" />
                </div>
                <div class="input-wrap">
                    <label for="password">Password</label>
                    <input type="password" name="password" />
                </div>
                <button type="submit">Log In</button>
                <a href="/register" class="switch" >Register</a>
            </form>
        </div>
    </body>
</html>